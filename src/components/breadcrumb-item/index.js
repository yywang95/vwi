import WiBreadcrumbItem from '../breadcrumb/breadcrumb-item.vue'

WiBreadcrumbItem.install = function (Vue) {
  Vue.component(WiBreadcrumbItem.name, WiBreadcrumbItem)
}

export default WiBreadcrumbItem
